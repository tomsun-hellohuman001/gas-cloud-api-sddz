<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sddz.gasstation.dao.mapper.AdminChinaAreaMapper">

  <select id="getRegionByPid" resultType="com.sddz.gasstation.entity.PC.AdminChinaArea">
    SELECT
      id,
      `NAME`,
      pid,
      sort,
      `LEVEL`,
      longcode,
      `CODE`
  FROM
      `admin_china_area`
  WHERE
      pid = #{pid}
      order by sort asc
  </select>

</mapper>
